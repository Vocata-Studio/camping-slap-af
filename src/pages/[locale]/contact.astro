---
import Layout from "../../layouts/Layout.astro";
import Hero from "../../components/Hero.astro";
import { contactUi, locales, isValidLocale, t } from "../../i18n/utils";
import type { Locale } from "../../i18n/utils";

const LEAFLET_VERSION = "1.9.4";

export function getStaticPaths() {
  return locales.map((locale) => ({ params: { locale } }));
}

const { locale } = Astro.params;
if (!isValidLocale(locale)) return Astro.redirect("/da");
const l = locale as Locale;

const heroImage = "/images/overhead-shot.webp";

const phone = t(l, "Contact", "phone");
const email = t(l, "Contact", "email");
const phoneHref = `tel:${phone.replace(/\s+/g, "")}`;
const copyUi = contactUi(l);
const formSuccess = t(l, "Contact", "formSuccess");
const formError = t(l, "Contact", "formError");
---

<Layout title={t(l, "Contact", "title")} locale={l} description={t(l, "Meta", "contactDescription")} preloadImage={heroImage}>
  <link
    slot="head"
    rel="stylesheet"
    href={`https://unpkg.com/leaflet@${LEAFLET_VERSION}/dist/leaflet.css`}
  />
  <Hero
    image={heroImage}
    imageAlt={t(l, "Contact", "title")}
    title={t(l, "Contact", "title")}
    subtitle={t(l, "Contact", "subtitle")}
    compact
  />

  <!-- Contact info + form -->
  <section class="section">
    <div class="container contact-grid">
      <!-- Info column -->
      <div class="contact-info">
        <h2 class="contact-info__heading">{t(l, "Contact", "infoTitle")}</h2>

        <ul class="contact-info__list">
          <li>
            <div class="contact-info__icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round">
                <path d="M20 10c0 7-8 12-8 12s-8-5-8-12a8 8 0 0 1 16 0Z" />
                <circle cx="12" cy="10" r="3" />
              </svg>
            </div>
            <div>
              <div class="contact-info__label">{t(l, "Contact", "addressLabel")}</div>
              <div class="contact-info__value">{t(l, "Contact", "address")}</div>
            </div>
          </li>
          <li>
            <div class="contact-info__icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round">
                <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92Z" />
              </svg>
            </div>
            <div>
              <div class="contact-info__label">{t(l, "Booking", "phoneLabel")}</div>
              <a
                href={phoneHref}
                class="contact-info__value contact-info__value--link contact-info__copy-trigger"
                data-copy-value={phone}
                data-copy-toast={copyUi.phoneCopied}
                data-copy-mobile-call="true"
              >
                <span>{phone}</span>
                <span class="contact-copy-subtext">{copyUi.copy}</span>
              </a>
            </div>
          </li>
          <li>
            <div class="contact-info__icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round">
                <rect width="20" height="16" x="2" y="4" rx="2" />
                <path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7" />
              </svg>
            </div>
            <div>
              <div class="contact-info__label">{t(l, "Booking", "emailLabel")}</div>
              <button
                type="button"
                class="contact-info__value contact-info__value--link contact-info__copy-trigger"
                data-copy-value={email}
                data-copy-toast={copyUi.emailCopied}
              >
                <span>{email}</span>
                <span class="contact-copy-subtext">{copyUi.copy}</span>
              </button>
            </div>
          </li>
          <li>
            <div class="contact-info__icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round">
                <circle cx="12" cy="12" r="10" />
                <polyline points="12 6 12 12 16 14" />
              </svg>
            </div>
            <div>
              <div class="contact-info__label">{t(l, "Contact", "hoursTitle")}</div>
              <div class="contact-info__value">{t(l, "Contact", "hoursText")}</div>
            </div>
          </li>
        </ul>
      </div>

      <!-- Form column -->
      <div class="contact-form">
        <h2 class="contact-form__heading">{t(l, "Contact", "formTitle")}</h2>
        <form id="contact-form" class="contact-form__fields" novalidate>
          <div class="field-row">
            <div class="field">
              <label for="contact-name">{t(l, "Contact", "nameLabel")}</label>
              <input type="text" id="contact-name" name="name" autocomplete="name" required />
            </div>

            <div class="field">
              <label for="contact-email">{t(l, "Contact", "emailLabel")}</label>
              <input type="email" id="contact-email" name="email" autocomplete="email" required />
            </div>
          </div>

          <div class="field">
            <label for="contact-phone">{t(l, "Contact", "phoneLabel")}</label>
            <input type="tel" id="contact-phone" name="phone" autocomplete="tel" />
          </div>

          <div class="field">
            <label for="contact-message">{t(l, "Contact", "messageLabel")}</label>
            <textarea id="contact-message" name="message" rows="5" required></textarea>
          </div>

          <div class="contact-form__actions">
            <button type="submit" class="btn contact-form__submit" data-submit-btn>
              <span class="btn-label">{t(l, "Contact", "sendButton")}</span>
              <span class="btn-spinner" aria-hidden="true">
                <span></span><span></span><span></span>
              </span>
            </button>
            <p data-form-status role="status" aria-live="polite" class="form-status hidden"></p>
          </div>
        </form>

        <div class="form-success-panel hidden" data-success-panel aria-live="polite">
          <div class="form-success-panel__icon" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10" />
              <polyline points="9 12 11.5 14.5 15 10" />
            </svg>
          </div>
          <h3 class="form-success-panel__title" data-success-title></h3>
          <p class="form-success-panel__body" data-success-body></p>
        </div>
      </div>
    </div>
  </section>

  <hr class="hairline" />

  <!-- Map placeholder -->
  <section class="section">
    <div class="container">
      <div class="section__head section__head--left">
        <h2>{t(l, "Contact", "mapTitle")}</h2>
        <p>{t(l, "Contact", "mapDescription")}</p>
      </div>
      <div id="csa-map" class="map-embed" aria-label={t(l, "Contact", "mapTitle")}></div>

      <script is:inline>
        (function () {
          var LAT = 56.930468;
          var LNG = 8.870665;

          function initMap() {
            if (!document.getElementById("csa-map")) return;
            var map = L.map("csa-map", {
              center: [LAT, LNG],
              zoom: 14,
              scrollWheelZoom: false,
            });

            L.tileLayer(
              "https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png",
              {
                attribution:
                  '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors &copy; <a href="https://carto.com/attributions">CARTO</a>',
                subdomains: "abcd",
                maxZoom: 19,
              }
            ).addTo(map);

            var pinIcon = L.divIcon({
              className: "csa-pin",
              html: '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 28 36" fill="none"><path d="M14 0C6.268 0 0 6.268 0 14c0 10.5 14 22 14 22S28 24.5 28 14C28 6.268 21.732 0 14 0z" fill="#2B9854"/><circle cx="14" cy="14" r="6" fill="white"/><circle cx="14" cy="14" r="3.5" fill="#2B9854"/></svg>',
              iconSize: [28, 36],
              iconAnchor: [14, 36],
              popupAnchor: [0, -38],
            });

            L.marker([LAT, LNG], { icon: pinIcon })
              .addTo(map)
              .bindPopup(
                "<strong>Camping Slap Af</strong><br>Kærvang 7, Sejerslev<br>7900 Nykøbing M"
              )
              .openPopup();
          }

          var script = document.createElement("script");
          script.src = "https://unpkg.com/leaflet@1.9.4/dist/leaflet.js";
          script.onload = initMap;
          script.onerror = function () {
            console.error("Leaflet failed to load");
          };
          document.head.appendChild(script);
        })();
      </script>
    </div>
  </section>
</Layout>

<script is:inline define:vars={{ formSuccess, formError }}>
  var form = document.getElementById('contact-form');
  if (form) {
    var status = form.querySelector('[data-form-status]');
    var submit = form.querySelector('[data-submit-btn]');
    var successPanel = document.querySelector('[data-success-panel]');

    // Inline validation on blur for required fields
    form.querySelectorAll('input[required], textarea[required]').forEach(function(field) {
      field.addEventListener('blur', function() {
        if (!field.value.trim()) {
          field.classList.add('field--invalid');
        } else {
          field.classList.remove('field--invalid');
        }
      });
      field.addEventListener('input', function() {
        if (field.value.trim()) field.classList.remove('field--invalid');
      });
    });

    function setLoading(loading) {
      if (!submit) return;
      submit.disabled = loading;
      submit.classList.toggle('btn--loading', loading);
    }

    function shakeError() {
      if (!submit) return;
      submit.classList.add('btn--shake');
      submit.addEventListener('animationend', function() {
        submit.classList.remove('btn--shake');
      }, { once: true });
    }

    function showSuccess() {
      if (!successPanel) return;
      var title = successPanel.querySelector('[data-success-title]');
      var body = successPanel.querySelector('[data-success-body]');
      var sepIdx = formSuccess.indexOf('! ');
      if (sepIdx !== -1) {
        if (title) title.textContent = formSuccess.slice(0, sepIdx + 1);
        if (body) body.textContent = formSuccess.slice(sepIdx + 2);
      } else {
        if (title) title.textContent = formSuccess;
      }
      var formWrapper = form.closest('.contact-form');
      if (formWrapper) formWrapper.classList.add('form--sent');
      successPanel.classList.remove('hidden');
      // Trigger transition on next frame
      requestAnimationFrame(function() {
        successPanel.classList.add('form-success-panel--visible');
      });
    }

    form.addEventListener('submit', function(event) {
      event.preventDefault();
      var data = new FormData(form);
      var payload = {
        name: String(data.get('name') || ''),
        email: String(data.get('email') || ''),
        phone: String(data.get('phone') || ''),
        message: String(data.get('message') || ''),
      };

      setLoading(true);
      if (status) status.classList.add('hidden');

      fetch('/api/contact', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload),
      }).then(function(res) {
        if (!res.ok) throw new Error('status ' + res.status);
        showSuccess();
        form.reset();
      }).catch(function(err) {
        console.error('contact submit failed', err);
        shakeError();
        if (status) {
          status.textContent = formError;
          status.classList.remove('hidden', 'form-status--success');
          status.classList.add('form-status--error');
        }
      }).finally(function() {
        setLoading(false);
      });
    });
  }
</script>

<style>
  .contact-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-10);
  }
  @media (min-width: 900px) {
    .contact-grid {
      grid-template-columns: 1fr 1.2fr;
      gap: var(--space-12);
    }
  }

  /* Info column */
  .contact-info__heading {
    font-size: var(--fs-xl);
    margin-bottom: var(--space-7);
  }
  .contact-info__list {
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
  }
  .contact-info__list > li {
    display: flex;
    align-items: flex-start;
    gap: var(--space-4);
  }
  .contact-info__icon {
    width: 44px;
    height: 44px;
    flex-shrink: 0;
    border-radius: var(--radius-md);
    background: var(--color-cream);
    color: var(--color-green);
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .contact-info__icon svg {
    width: 22px;
    height: 22px;
  }
  .contact-info__label {
    font-family: var(--font-heading);
    font-weight: 600;
    font-size: var(--fs-sm);
    color: var(--color-ink);
    margin-bottom: 2px;
  }
  .contact-info__value {
    color: var(--color-muted);
    font-size: var(--fs-sm);
    line-height: var(--lh-body);
  }
  .contact-info__value--link {
    color: var(--color-muted);
  }
  .contact-info__value--link:hover {
    color: var(--color-green);
  }
  .contact-info__copy-trigger {
    display: inline-flex;
    align-items: baseline;
    justify-content: space-between;
    gap: var(--space-4);
    min-width: min(100%, 24rem);
    text-align: left;
  }
  .contact-info__copy-trigger:hover .contact-copy-subtext {
    color: var(--color-green);
  }

  /* Form column */
  .contact-form {
    background: var(--color-cream);
    border-radius: var(--radius-lg);
    padding: var(--space-7);
    display: flex;
    flex-direction: column;
    gap: var(--space-5);
  }
  .contact-form__heading {
    font-size: var(--fs-xl);
    margin: 0 0 var(--space-2);
  }
  .contact-form__fields {
    display: flex;
    flex-direction: column;
    gap: var(--space-5);
  }
  .field-row {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-5);
  }
  @media (min-width: 600px) {
    .field-row {
      grid-template-columns: 1fr 1fr;
    }
  }
  .field {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    width: 100%;
  }
  .field label {
    font-family: var(--font-body);
    font-size: var(--fs-sm);
    font-weight: 600;
    color: var(--color-ink);
  }
  .field input,
  .field textarea {
    width: 100%;
    font: inherit;
    font-size: var(--fs-base);
    color: var(--color-ink);
    background: var(--color-bg);
    border: 1px solid var(--color-hairline-strong);
    border-radius: var(--radius-md);
    padding: 0.7rem 0.9rem;
    line-height: 1.4;
    resize: vertical;
    transition: border-color 0.15s ease, box-shadow 0.15s ease;
  }
  .field input:focus,
  .field textarea:focus {
    outline: none;
    border-color: var(--color-green);
    box-shadow: 0 0 0 3px rgba(43, 152, 84, 0.18);
  }
  .field input.field--invalid,
  .field textarea.field--invalid {
    border-color: #c0392b;
    box-shadow: 0 0 0 3px rgba(192, 57, 43, 0.14);
  }
  .contact-form__actions {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    margin-top: var(--space-1);
  }
  .contact-form__submit {
    align-self: flex-start;
  }

  @media (min-width: 720px) {
    .contact-form__actions {
      align-items: flex-start;
    }
  }

  /* Map */
  .map-embed {
    width: 100%;
    height: 420px;
    border-radius: var(--radius-lg);
    overflow: hidden;
    border: 1px solid var(--color-hairline);
  }

  :global(.csa-pin) {
    background: none;
    border: none;
  }
  :global(.csa-pin svg) {
    width: 28px;
    height: 36px;
    display: block;
    filter: drop-shadow(0 2px 6px rgba(0, 0, 0, 0.22));
  }
  :global(.leaflet-popup-content-wrapper) {
    border-radius: 10px;
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.12);
    font-family: var(--font-body);
    font-size: 13px;
  }
  :global(.leaflet-popup-content strong) {
    font-family: var(--font-heading);
    font-weight: 600;
  }

  /* Button loading + shake */
  .contact-form__submit .btn-spinner {
    display: none;
    align-items: center;
    gap: 4px;
  }
  .contact-form__submit .btn-spinner span {
    display: inline-block;
    width: 5px;
    height: 5px;
    border-radius: 50%;
    background: currentColor;
    opacity: 0.5;
    animation: dot-pulse 1.2s ease-in-out infinite;
  }
  .contact-form__submit .btn-spinner span:nth-child(2) { animation-delay: 0.2s; }
  .contact-form__submit .btn-spinner span:nth-child(3) { animation-delay: 0.4s; }
  @keyframes dot-pulse {
    0%, 80%, 100% { transform: scale(0.75); opacity: 0.4; }
    40% { transform: scale(1); opacity: 1; }
  }
  .btn--loading .btn-label { display: none; }
  .btn--loading .btn-spinner { display: inline-flex; }
  .btn--loading { opacity: 0.85; cursor: default; }

  @keyframes shake {
    0%, 100% { transform: translateX(0); }
    20% { transform: translateX(-5px); }
    40% { transform: translateX(5px); }
    60% { transform: translateX(-4px); }
    80% { transform: translateX(4px); }
  }
  .btn--shake { animation: shake 0.4s ease; }

  /* Success panel */
  .form--sent .contact-form__heading,
  .form--sent .contact-form__fields { display: none; }

  .form-success-panel {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: var(--space-4);
    padding: var(--space-8) var(--space-4);
    opacity: 0;
    transform: translateY(8px);
    transition: opacity 0.35s ease, transform 0.35s ease;
  }
  .form-success-panel.hidden { display: none; }
  .form-success-panel--visible {
    opacity: 1;
    transform: translateY(0);
  }
  .form-success-panel__icon {
    width: 56px;
    height: 56px;
    border-radius: 50%;
    background: rgba(43, 152, 84, 0.12);
    color: var(--color-green);
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .form-success-panel__icon svg {
    width: 30px;
    height: 30px;
    stroke-dasharray: 60;
    stroke-dashoffset: 60;
    animation: draw-check 0.5s ease 0.1s forwards;
  }
  @keyframes draw-check {
    to { stroke-dashoffset: 0; }
  }
  .form-success-panel__title {
    font-size: var(--fs-lg);
    font-family: var(--font-heading);
    font-weight: 600;
    color: var(--color-ink);
    margin: 0;
  }
  .form-success-panel__body {
    font-size: var(--fs-sm);
    color: var(--color-muted);
    line-height: var(--lh-body);
    margin: 0;
  }

  .form-status {
    font-size: var(--fs-sm);
    line-height: var(--lh-body);
  }
  .form-status.hidden { display: none; }
  .form-status--success { color: var(--color-green); }
  .form-status--error { color: #c0392b; }
</style>
